@php
    $opacite = ($data['overlay_opacity'] ?? 40) / 100;
    $aUneImage = $media->isNotEmpty();
@endphp
<section class="relative w-full min-h-[480px] flex items-center justify-center text-center overflow-hidden {{ $aUneImage ? '' : 'bg-gray-800' }}">
    @if($aUneImage)
        <img
            src="{{ Storage::url($media->first()->path) }}"
            alt="{{ $data['alt'] ?? '' }}"
            class="absolute inset-0 w-full h-full object-cover"
        >
        <div class="absolute inset-0 bg-black" style="opacity: {{ $opacite }}"></div>
    @endif

    <div class="relative z-10 max-w-2xl px-4 py-16 text-white">
        <h1 class="text-3xl md:text-5xl font-bold mb-4">{{ $data['titre'] }}</h1>

        @if(!empty($data['sous_titre']))
            <p class="text-lg md:text-xl mb-6">{{ $data['sous_titre'] }}</p>
        @endif

        @if(!empty($data['bouton_texte']) && !empty($data['bouton_url']))
            
                href="{{ $data['bouton_url'] }}"
                class="inline-block bg-white text-black font-semibold px-6 py-3 rounded hover:bg-gray-100 transition"
            >
                {{ $data['bouton_texte'] }}
            </a>
        @endif
    </div>
</section>
